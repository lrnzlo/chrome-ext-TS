{
  "name": "chrome-ext-ts",
  "private": false,
  "version": "1.0",
  "scripts": {
    "build": "rm -r dist && ts-node esbuild.ts && cp manifest.json ./dist && cp -r assets ./dist && cp -r src/public ./dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.178",
    "@types/node": "^17.0.18",
    "esbuild": "^0.14.22",
    "path": "^0.12.7",
    "typescript": "^4.5.4"
  },
  "dependencies": {}
}